# Memory-Bank Fact-Check vs. Repo — 2026-09-23

Read-only audit. Repo root: `I:\parp\parp-tools\wow-viewer`, branch `v0.6.0-dev`. Ground truth
sources: `WowViewer.slnx`, `.csproj` files, `Directory.Build.props`, `.gitmodules`, `src/**/*.cs`,
`specs/` directory listing.

---

## projectbrief.md

| Claim | Verdict | Evidence |
|---|---|---|
| Readers/contracts in `src/core/`, viewer in `src/viewer/WoWViewer/`, CLI tools in `src/tools/` | STALE (partial) | CLI tools actually live at `wow-viewer/tools/` (top-level), not `src/tools/` — confirmed by `WowViewer.slnx` folders `/tools/inspect/`, `/tools/converter/`, etc., and by `AGENTS.md` §4 which says `wow-viewer/tools/`. `projectbrief.md`'s own line 18 has the same error. |
| "Optional data-harvester workflows" | ACCURATE | `data-harvester/` exists with `pyproject.toml`. |
| `H:\CLIENTS` approved library, no hardcoded client paths | ACCURATE (per AGENTS.md, consistent) | Matches AGENTS.md §5 and `data-paths.md`. |
| Continuity contract (activeContext/progress/workstream-*) | ACCURATE | Files present: `activeContext.md`, `progress.md`, 7 `workstream-*.md` files, `archive/`. |

## systemPatterns.md

| Claim | Verdict | Evidence |
|---|---|---|
| FourCC read/write reversal pattern | ACCURATE (pattern-level) | Confirmed independently via `coding_standards.md` FourCC section language; no counter-evidence found. |
| Terrain adapters: only `AlphaTerrainAdapter` and `StandardTerrainAdapter` | STALE — incomplete | Two more adapters exist and are not mentioned: `src/viewer/WoWViewer/Terrain/AhdrTerrainAdapter.cs` (CASC/AHDR-backed) and `src/viewer/WoWViewer/Terrain/RosettaDatastoreTerrainAdapter.cs`. Also `AlphaTerrainAdapter` exists in **two** places — `src/viewer/WoWViewer/Terrain/AlphaTerrainAdapter.cs` and `src/core/WowViewer.Core.IO/Maps/AlphaTerrainAdapter.cs` — the doc doesn't distinguish them. |
| Liquid pipeline (MCLQ/MH2O/MLIQ) | UNVERIFIABLE (not re-derived; no contradicting evidence found) | Not directly re-checked line-by-line; no counter-evidence surfaced during this audit. |
| AOI streaming: `TerrainManager`, `SemaphoreSlim(4)`, `_tileCache`, "legacy MdxViewer path" | ACCURATE, with a nuance | `src/viewer/WoWViewer/Terrain/TerrainManager.cs:75-76`: `private const int MaxConcurrentMpqReads = 4; ... new SemaphoreSlim(MaxConcurrentMpqReads)`; field `_tileCache` confirmed (line 35, `ConcurrentDictionary`). However `TerrainManager` itself lives in the **active** `src/viewer/WoWViewer/` project, not in the genuinely defunct one — `MdxViewer` symbol references now concentrate in a project literally named `src/viewer/WowViewer.App.Defunct/` (5 of 7 hits). The "legacy" framing is directionally right but `TerrainManager` is not itself quarantined there. |
| PM4 cache: in-memory `Pm4PerFileCache` cap 256, LRU-soft | ACCURATE | `src/core/WowViewer.Core.PM4/Caching/pm4PerFileCache.cs:21`: `public Pm4PerFileCache(int capacity = 256)`; doc comment confirms "soft" LRU cap. |
| PM4 cache: on-disk `Pm4PerFileCacheService`, magic `PM4F`, **version 8** | STALE | `src/core/WowViewer.Core.PM4/Caching/pm4PerFileCacheService.cs:29,45`: `EntryMagic = "PM4F"` is correct, but `EntryVersion = 9`, not 8. The file's own doc comment (line 22) also still says "version 8" — the code was bumped without updating either doc. A sibling file, `src/viewer/WoWViewer/Terrain/Pm4OverlayCacheService.cs:24`, independently confirms `CacheVersion = 9` and has a comment noting the bump "to 9 in the same change." |
| On-disk path `output/cache/pm4-overlay/{segment}/{map}/files/` | ACCURATE | Confirmed literally in `pm4PerFileCacheService.cs:13` doc comment and `Pm4OverlayCacheService.cs:54` (`Path.Combine(..., "output", "cache", "pm4-overlay", cacheSegment)`). |
| M2 pipeline: `M2ModelReaderDispatcher.ReadDetailed` → era detection → `M2SequenceAliasResolver` → `M2TrackSampler` → `M2BonePoseEvaluator` | STALE (one class renamed/missing) | `M2ModelReaderDispatcher` (in `Core.IO/M2Chunked/`), `M2TrackSampler`, and `M2BonePoseEvaluator` (both in `Core.Runtime/M2/`) all exist as named. **`M2SequenceAliasResolver` does not exist anywhere in `src/`** (`class M2SequenceAliasResolver` — 0 matches). The closest live classes are `M2ExternalAnimationRuntime` / `M2ExternalAnimationRuntimeState` in `Core.Runtime/M2/`, which may be the renamed/successor component, but the exact name in the doc is gone. |
| Coordinate system formulas | UNVERIFIABLE (not re-derived from render code in this pass) | No contradicting evidence found; out of scope to re-derive the full transform chain in a read-only fact-check. |
| Spec Kit workflow commands | ACCURATE | Matches `AGENTS.md` §1 and the `speckit-*` skills present in the environment. |
| Pose farm pipeline (spec 053) | UNVERIFIABLE | Spec 053 not directly checked for existence in this pass (out of the explicit check list); class names (`BvhDocumentBuilder`, `BvhDocumentWriter`, `PoseClipBuilder`, `PoseLibraryIndexBuilder`) not individually re-verified. |

## techContext.md

| Claim | Verdict | Evidence |
|---|---|---|
| Runtime: .NET 10, C# 13 | STALE (language version) | `TargetFramework` confirmed `net10.0` (`src/core/WowViewer.Core/WowViewer.Core.csproj`). But `Directory.Build.props:12` sets `<LangVersion>preview</LangVersion>`, not a pinned "13". The repo is not building against a fixed C# 13; it opts into preview language features (net10.0's default major version would be C# 14 absent an override). |
| Build command `dotnet build wow-viewer/WowViewer.slnx -c Debug` | ACCURATE | Matches `AGENTS.md` §6 exactly; `WowViewer.slnx` exists at that path. |
| Graphics: OpenGL 3.3 via Silk.NET; Vulkan/WebGL future | UNVERIFIABLE (not re-derived from renderer init code) | No contradicting evidence found. |
| Testing: xUnit, only `Core.Tests`, `Core.PM4.Tests`, `Core.Anim.Tests` | STALE — incomplete list | `WowViewer.slnx` and `tests/` directory show **two more** test projects: `tests/WowViewer.Core.Curation.Tests/WowViewer.Core.Curation.Tests.csproj` and `tests/WowViewer.Core.Editor.Tests/WowViewer.Core.Editor.Tests.csproj` (the latter has subfolders `Eras/`, `Integrity/`, `Operations/`, `Plugins/`, `Reconciliation/`, `Session/` — a substantial test surface, entirely unlisted). |
| Key Projects table: `WowViewer.Core`, `.Core.IO`, `.Core.Runtime`, `.Core.PM4`, `.Core.Anim` | STALE — incomplete list | `WowViewer.slnx` shows **three more core libraries** not listed: `src/core/WowViewer.Core.Curation/` (Parquet.Net-based dataset curation), `src/core/WowViewer.Core.Editor/` (editor contracts/operations — 50 `.cs` files across `Bridge/`, `Eras/`, `Integrity/`, `Logging/`, `Operations/`, `Plugins/`, `Session/`, `Staging/`), and `src/core/WowViewer.Core.Renderer/` (Silk.NET OpenGL + SereniaBLPLib + ImageSharp — rendering code pulled out of the viewer app into a core library). Also missing: `src/tools-shared/WowViewer.Tools.Shared/` (shared CLI-tool project, referenced by `WowViewer.slnx` under `/src/tools-shared/`). |
| CLI tools list: Inspect, Converter, Harvest, Capture | STALE — incomplete list | `WowViewer.slnx` / `tools/` directory show **four more** tool projects: `WowViewer.Tool.ValidationCapture` (`tools/validation-capture/`), `WowViewer.Tool.MaskValidate` (`tools/mask-validate/`), `WowViewer.Tool.WdlRead` (`tools/wdl-read/`), `WowViewer.Tool.WmoMinimap` (`tools/wmo-minimap/`) — plus `WowViewer.Tool.V22Enrich` (`tools/enrich/`), which doesn't even appear in `WowViewer.slnx`'s folder list (present on disk, not wired into the solution — itself worth flagging in the target repo, not just the doc). |
| `WowViewer.Tool.AnimFarm` "does not exist... `tools/animfarm/` is an empty solution folder" | ACCURATE | `WowViewer.slnx` confirms `<Folder Name="/tools/animfarm/" />` with zero `<Project>` children — still true as of this audit. |
| Format Readers table — `M2ModelReader` (classic MD20) Complete | ACCURATE | `src/core/WowViewer.Core.IO/M2/M2ModelReader.cs` exists. |
| `M2ChunkedModelReader` in progress (spec 048) | STALE — dangling spec pointer | Class exists (`src/core/WowViewer.Core.IO/M2Chunked/M2ChunkedModelReader.cs`), but **spec 048 does not exist anywhere in the repo** — `specs/` numbering jumps `009 → 046 → ... → 065 ...`, no `048-*` directory active or archived. The current owning context for M2 era/reader work is `specs/154-m2-era-reader-parity/` (itself marked "Superseded 2026-09-10" and absorbed into `specs/235-legacy-mdx-m2-rendering/`). Neither status claim ("in progress" / "complete") nor the spec-048 pointer can be verified against a real spec. |
| `M2Era1121ModelReader` Complete (spec 048) | STALE — same dangling spec-048 pointer | Class exists (`src/core/WowViewer.Core.IO/M2Era1121/M2Era1121ModelReader.cs`); spec 048 pointer unverifiable as above. |
| WMO reader: `WmoDetailReader`, `WmoSummaryReader` Complete | STALE — wrong class name | `WmoSummaryReader` exists (`src/core/WowViewer.Core.IO/Wmo/WmoSummaryReader.cs`). **`WmoDetailReader` does not exist.** The WMO detail-reading surface is instead split across ~15 specialized readers: `WmoGroupMeshDetailReader`, `WmoMaterialDetailReader`, `WmoLightDetailReader`, `WmoPortalDetailReader`, `WmoDoodadDetailReader`, `WmoEmbeddedGroupDetailReader`, `WmoEmbeddedGroupMeshDetailReader`, plus dozens of `*SummaryReader` classes, aggregated via `WmoRenderDocumentReader.cs`. |
| DBC/DB2 reader: `DbcLookup`, `DbClientFileReader` Complete | STALE — wrong class name | `DbClientFileReader` exists (`src/core/WowViewer.Core.IO/Files/DbClientFileReader.cs`). **`DbcLookup` does not exist anywhere in `src/`.** Current DBC surface: `DbcReader.cs`, `DbcTableLoader.cs`, `ArchiveReaderDbcProvider.cs`, `RosettaDbcGenerator.cs`, plus table-specific readers (`DbcLiquidMaterialTable.cs`, `DbcLiquidObjectTable.cs`, `DbcLiquidTypeTable.cs`, `DbcMapPhaseTable.cs`, `MapDirectoryLookup.cs`, `AreaIdMapper.cs`, `GroundEffectLookup.cs`), all under `src/core/WowViewer.Core.IO/Dbc/`. |
| ADT reader: `AlphaTerrainAdapter`, `StandardTerrainAdapter` Complete | STALE — incomplete (see systemPatterns.md row above) | `AhdrTerrainAdapter` and `RosettaDatastoreTerrainAdapter` also exist and are Complete (see systemPatterns.md). |
| MPQ: `MpqArchiveCatalog`, `AlphaArchiveReader` Complete | ACCURATE | Both confirmed: `src/core/WowViewer.Core.IO/Files/MpqArchiveCatalog.cs` (`sealed class MpqArchiveCatalog`), `src/core/WowViewer.Core.IO/Files/AlphaArchiveReader.cs` (`static class AlphaArchiveReader`). Also present but unlisted: `NativeMpqService` / `NativeMpqServiceFactory` in the same folder — an alternative/native MPQ path not mentioned in the doc at all. |
| External Dependencies: Silk.NET, ImGui.NET, SereniaBLPLib, ImageSharp, DBCD, Warcraft.NET, GillijimProject, PyTorch, Zarr-Python | STALE — missing entries, one mislabeled | `.gitmodules` (repo root) confirms these registered submodules under `wow-viewer/libs/` that are **absent from the list**: `wowdev/TACTSharp` (CASC/TACT client-data access — referenced by `WowViewer.Core.IO.csproj`), `wowdev/wow-listfile`, `wowdev/WoWDBDefs` (DBC schema defs, bundled with the viewer per `WoWViewer.csproj` comment "required for DBC parsing via DBCD"), `Marlamin/WoWTools.Minimaps`. Additionally, `libs/WoW-Tools/GillijimProject` (used by the doc's own "GillijimProject: ... (libs/ submodule)" line) is **not** listed in `.gitmodules` for any `wow-viewer/libs/...` path — the registered gillijimproject submodules all live under unrelated trees (`PM4Tool/docs/apps/gillijimproject`, `parpToolbox/...`). So the wow-viewer copy is vendored/copied, not an actual git submodule as the doc states. Also unmentioned: `libs/alpha-core` (has its own nested `.git`, not a registered submodule either) and a duplicate, apparently-unused `libs/WoW-Tools/DBCD` (all real `DBCD` project references in `.csproj` files point to `libs/wowdev/DBCD`, not `libs/WoW-Tools/DBCD`). |

## coding_standards.md

| Claim | Verdict | Evidence |
|---|---|---|
| Project Layout tree: `src/core/` lists only `Core`, `Core.IO`, `Core.PM4`, `Core.Runtime`, `Core.Anim` | STALE — incomplete | Same gap as techContext.md: missing `WowViewer.Core.Curation`, `WowViewer.Core.Editor`, `WowViewer.Core.Renderer` (all real, wired into `WowViewer.slnx`), and `src/tools-shared/WowViewer.Tools.Shared/` (a sibling of `src/core/` and `src/viewer/`, entirely absent from the tree diagram). |
| "The viewer app is the only consumer that is allowed to add UI code" / "the only UI project" | ACCURATE (as a rule); the project list around it is stale | The rule itself isn't contradicted — `WowViewer.Core.Renderer` is OpenGL/rendering infrastructure, not ImGui UI — but the doc's project inventory doesn't acknowledge that rendering code now also lives in a core library, which readers could easily read as a UI-boundary violation without the context. |
| `H:\CLIENTS` approved / staging policy | ACCURATE | Consistent with `AGENTS.md` and `data-paths.md`. |
| `WowViewer.Core.Anim.PathNormalizer` canonical path helper | UNVERIFIABLE (not directly grepped in this pass) | Not re-checked for existence; no contradicting evidence found. |
| Memory Bank Layout table (file responsibilities) | ACCURATE | Matches actual file set and content style seen in `activeContext.md`/`progress.md`/`workstream-*.md` during this audit. |

## data-paths.md

| Claim | Verdict | Evidence |
|---|---|---|
| Override env vars: `WOWVIEWER_WORKSPACE`, `WOWVIEWER_CACHE`, `WOWVIEWER_DATASETS`, `WOWVIEWER_TMP`, `WOWVIEWER_TEST_DATA`, `WOWVIEWER_STAGED_CLIENTS`, `WOWARCHIVE_MOUNT` | STALE — none implemented in C#, none found in Python | Exhaustive `Environment.GetEnvironmentVariable(...)` grep across `src/**/*.cs` and `tools/**/*.cs` finds **zero** occurrences of any of these seven names. The only env vars actually read in the codebase are: `WOWVIEWER_PM4_PROFILE` (undocumented, `src/viewer/WoWViewer/Logging/pm4Profiling.cs:18`), `PARP_MDX_TEXTURE_DIAG`, `PARP_M2_ENABLE_ANIMATION`, `PARP_M2_FORCE_SOLID`, `PARP_MDX_DEBUG` (all in `Rendering/ModelRenderer.cs`), and a native-renderer setting var in `WowViewerM2RuntimeBridge.cs`. A parallel grep of `data-harvester/` for the same seven names also returned zero matches. The "How Overrides Resolve" section describes a resolution order (CLI arg → env var → convention default) that does not appear to be wired up for any of the documented path variables. |
| `output/cache/pm4-overlay/{segment}/{map}/files/` pattern | ACCURATE | Confirmed in `pm4PerFileCacheService.cs` doc comment and `Pm4OverlayCacheService.cs:54` (see systemPatterns.md row above). |
| Cache root resolved as `AppDomain.CurrentDomain.BaseDirectory/output/cache/...` | Nuance not in doc | `Pm4OverlayCacheService.cs:54` resolves the cache root from the app's base directory, not from a `WOWVIEWER_CACHE` env var or workspace-root convention as the doc implies — reinforcing that the override mechanism described isn't actually wired to this code path. |
| Test data paths (`test_data/development/...`, `test_data/0.5.3/`, `test_data/minimaps/development`) | UNVERIFIABLE | Directory existence not individually re-verified in this pass; no contradicting evidence found. |

## Workstream headers / owner-spec / status lines

| File | Claim | Verdict | Evidence |
|---|---|---|---|
| `workstream-alpha053-renderer-performance.md` | Owner: Spec 150; "Last updated: 2026-08-14" | ACCURATE (spec exists) | `specs/150-alpha-renderer-performance/` exists, active (not archived). |
| `workstream-area-overlay-053-ghidra.md` | "Spec 149's resident Zone/SubZone overlay"; "Last verified: 2026-08-14" | ACCURATE (spec exists, title differs but content matches) | `specs/149-pm4-region-audio-controls/` exists (title: "PM4 Region Navigation and Audio Trigger Controls" — doesn't literally say "Zone/SubZone", but `specs/149-pm4-region-audio-controls/{spec,tasks,research,data-model}.md` all contain zone/subzone-overlay content per grep, so the pointer is substantively correct even though it reads like a title mismatch at first glance). |
| `workstream-atmosphere-501-ghidra.md` | Owner specs 160 (skybox), 147 (fog), implied Ghidra project "Mists of Pandaria 5.0.1.15464"; "Last verified: 2026-09-02" | ACCURATE (specs exist) | `specs/160-skybox-rendering/` and `specs/147-minimap-fog-instancing/` both exist, active. Ghidra project/session details not independently re-verifiable (external tool state). |
| `workstream-audio-501-ghidra.md`, `workstream-audio-client-053-ghidra.md` | Cross-reference each other; era-split backend claim (DirectSound+DirectMusic vs FMOD) | UNVERIFIABLE | Native-binary claims from a Ghidra session; nothing in the managed C# repo to check this against. No owner-spec number stated in the header to verify against `specs/`. |
| `workstream-pm4-decode.md` | Owner specs 130, 131 (epic-pm4-restoration); "Last updated: 2026-08-04. Branch `131-pm4-scene-graph-doodads`" | ACCURATE (as dated historical note) | `specs/130-pm4-remaining-decode/` exists, active. Branch `131-pm4-scene-graph-doodads` still exists in git (`git branch --list` confirms) but current branch is `v0.6.0-dev` — the header is a dated snapshot, not a claim about the current branch, so this is not a correction, just worth noting the branch pointer is now historical. |
| `workstream-terrain-ml.md` | Owner specs 114, 125, 126, 111, 139, 140; "Last updated: 2026-08-10" | ACCURATE (specs exist) | All six spec directories (`114-`, `125-`, `126-`, `111-`, `139-`, `140-`) exist under `specs/`, none archived. |
| `weak-signal-tile-archaeology.md` | "Recorded: 2026-08-03. Status: tooling built, validated, and parked." — no explicit owner spec in the header | UNVERIFIABLE (no spec pointer to check) | No spec number given in the header to verify. |
| `pm4-restoration-epic.md` | Points to `specs/130-pm4-remaining-decode/`, `specs/129-pm4-zarr-dataset/`, `specs/128-pm4-negative-bsp-matching/`, `memory-bank/workstream-pm4-decode.md` | ACCURATE | All three spec directories exist and are active (not archived); `workstream-pm4-decode.md` exists. |

---

## Missing subsystems not covered by techContext.md / systemPatterns.md

- `src/core/WowViewer.Core.Editor/` — editor contracts/operations library (50 `.cs` files: `Bridge/`, `Eras/`, `Integrity/`, `Logging/`, `Operations/`, `Plugins/`, `Session/`, `Staging/`), backed by `tests/WowViewer.Core.Editor.Tests/`. Referenced in `AGENTS.md` §4 but absent from both memory-bank reference docs' project tables.
- `src/core/WowViewer.Core.Curation/` — dataset curation library (Parquet.Net dependency), backed by `tests/WowViewer.Core.Curation.Tests/`. Not mentioned anywhere in `techContext.md` or `coding_standards.md`.
- `src/core/WowViewer.Core.Renderer/` — OpenGL rendering code (Silk.NET.OpenGL, Silk.NET.Windowing, SixLabors.ImageSharp, SereniaBLPLib) extracted out of the viewer app into a core library. Not mentioned anywhere.
- `src/tools-shared/WowViewer.Tools.Shared/` — shared CLI-tool support library, wired into `WowViewer.slnx` under `/src/tools-shared/`. Not mentioned.
- CASC/AHDR data source: `src/viewer/WoWViewer/Terrain/AhdrTerrainAdapter.cs` and `src/viewer/WoWViewer/ViewerApp_CascAhdr.cs` — a CASC-backed terrain adapter, presumably built on the vendored `libs/wowdev/TACTSharp` submodule (which itself is entirely unmentioned in `techContext.md`'s dependency list). This is directly relevant to the "v0.6 release: CASC + modern clients" project line in the user's own memory.
- DAT layer / export: `src/viewer/WoWViewer/Terrain/DatLayerSource.cs`, `src/core/WowViewer.Core.IO/Maps/DatToLkAdtConverter.cs` — matches the two most recent commits on this branch ("Implement DAT layer source and export functionality", "Enhance DAT handling... for terrain rendering"), neither reflected in `systemPatterns.md`'s terrain-adapter section.
- Scene lights / ambient + local light management: `src/viewer/WoWViewer/Rendering/SceneLight.cs`, `SceneLightManager.cs`, `ISceneLightEmitter.cs`, `Terrain/LightService.cs`, `Terrain/TerrainLighting.cs`, `ViewerApp_Lighting.cs` — matches the "Enhance lighting system with ambient light support and local light management" commit; no mention in either reference doc.
- `RosettaDatastoreTerrainAdapter` (`src/viewer/WoWViewer/Terrain/RosettaDatastoreTerrainAdapter.cs`) — a fourth terrain adapter beyond the two `systemPatterns.md` documents.
- `NativeMpqService` / `NativeMpqServiceFactory` (`src/core/WowViewer.Core.IO/Files/NativeMpqService.cs`) — an alternative MPQ archive path alongside `MpqArchiveCatalog`, unmentioned.
- `WowViewer.App.Defunct` (`src/viewer/WowViewer.App.Defunct/`) — a project explicitly named "Defunct" that still holds the legacy `MdxViewer`-era bootstrap code (`WowViewerDesktopApp.cs`, `WowViewerWorldSessionBootstrapper.cs`, `WowViewerArchiveBootstrap.cs`, `WowViewerWorldRuntimeBridge.cs`). Neither doc names this project, and `systemPatterns.md`'s "legacy MdxViewer path" note doesn't point at it even though it would be the natural place for that label.
- Vendored CASC/TACT stack: `libs/wowdev/TACTSharp` (submodule, referenced from `WowViewer.Core.IO.csproj`) plus its siblings `TACTTool`, `TACTSharp.Native`, `TACTBench`, `TACTSharp.Tests` — entirely absent from `techContext.md`'s External Dependencies table.
- `libs/wowdev/WoWDBDefs` (submodule; DBC schema defs bundled with the viewer per its own `.csproj` comment), `libs/wowdev/wow-listfile` (submodule), `libs/Marlamin/WoWTools.Minimaps` (submodule) — none listed as dependencies.
- `libs/alpha-core` — a large vendored tree (server-side reference, `.git` present but not a registered submodule for this repo) sitting under `wow-viewer/libs/`; not documented anywhere in scope, and its provenance (submodule vs. stray clone) is itself worth a follow-up.

---

## Corrections to apply

1. **`projectbrief.md`** — old: "CLI tools live in `src/tools/`" (line 18). New: CLI tools live in `wow-viewer/tools/` (top-level, not under `src/`). Evidence: `WowViewer.slnx` folders `/tools/inspect/`, `/tools/converter/`, `/tools/harvest/`, `/tools/capture/`, etc.; `AGENTS.md` §4 states `wow-viewer/tools/`.

2. **`systemPatterns.md`** — old: "Two implementations for different WDT/ADT formats: AlphaTerrainAdapter, StandardTerrainAdapter." New: at least four terrain adapters exist — add `AhdrTerrainAdapter` (`src/viewer/WoWViewer/Terrain/AhdrTerrainAdapter.cs`, CASC-backed) and `RosettaDatastoreTerrainAdapter` (`src/viewer/WoWViewer/Terrain/RosettaDatastoreTerrainAdapter.cs`); also note `AlphaTerrainAdapter` exists in two places (`src/viewer/WoWViewer/Terrain/` and `src/core/WowViewer.Core.IO/Maps/`).

3. **`systemPatterns.md`** — old: "On-disk (`Pm4PerFileCacheService`, magic PM4F, version 8)". New: version is **9**, not 8. Evidence: `src/core/WowViewer.Core.PM4/Caching/pm4PerFileCacheService.cs:45` (`private const int EntryVersion = 9;`), corroborated by `src/viewer/WoWViewer/Terrain/Pm4OverlayCacheService.cs:24` (`CacheVersion = 9`, with a comment about the bump from 8 to 9).

4. **`systemPatterns.md`** — old: M2 pipeline step "`M2SequenceAliasResolver` (alias chain → terminal sequence)". New: this class does not exist in the current codebase (0 matches for `class M2SequenceAliasResolver` anywhere under `src/`); verify against current code and rename/remove, or point at whatever alias-resolution logic actually lives in `src/core/WowViewer.Core.Runtime/M2/` today (candidates: `M2ExternalAnimationRuntime.cs`, `M2ExternalAnimationRuntimeState.cs`).

5. **`techContext.md`** — old: "Runtime: .NET 10, C# 13". New: `Directory.Build.props:12` pins `<LangVersion>preview</LangVersion>`, not C# 13 — the project opts into preview language features rather than a fixed C# 13.

6. **`techContext.md`** — old: Testing row lists only `WowViewer.Core.Tests`, `WowViewer.Core.PM4.Tests`, `WowViewer.Core.Anim.Tests`. New: add `tests/WowViewer.Core.Curation.Tests/` and `tests/WowViewer.Core.Editor.Tests/` (the latter has substantial coverage: `Eras/`, `Integrity/`, `Operations/`, `Plugins/`, `Reconciliation/`, `Session/`). Evidence: `WowViewer.slnx` `/tests/` folder, `tests/` directory listing.

7. **`techContext.md`** — old: Key Projects table lists 5 core libraries (`Core`, `Core.IO`, `Core.Runtime`, `Core.PM4`, `Core.Anim`). New: add `WowViewer.Core.Curation` (`src/core/WowViewer.Core.Curation/`), `WowViewer.Core.Editor` (`src/core/WowViewer.Core.Editor/`), `WowViewer.Core.Renderer` (`src/core/WowViewer.Core.Renderer/`), and `WowViewer.Tools.Shared` (`src/tools-shared/WowViewer.Tools.Shared/`). Evidence: `WowViewer.slnx`.

8. **`techContext.md`** — old: CLI tools list has 4 entries (Inspect, Converter, Harvest, Capture). New: add `WowViewer.Tool.ValidationCapture` (`tools/validation-capture/`), `WowViewer.Tool.MaskValidate` (`tools/mask-validate/`), `WowViewer.Tool.WdlRead` (`tools/wdl-read/`), `WowViewer.Tool.WmoMinimap` (`tools/wmo-minimap/`); note `WowViewer.Tool.V22Enrich` (`tools/enrich/`) exists on disk but is **not** wired into `WowViewer.slnx`. Evidence: `WowViewer.slnx`, `tools/` directory listing.

9. **`techContext.md`** — old: "`M2ChunkedModelReader` ... In progress (spec 048)" and "`M2Era1121ModelReader` ... Complete (spec 048)". New: spec 048 does not exist anywhere in `specs/` (numbering jumps 009→046). Point instead at the actual current context: `specs/154-m2-era-reader-parity/` (superseded 2026-09-10, absorbed into `specs/235-legacy-mdx-m2-rendering/`), and re-verify each reader's actual completion status against that spec rather than repeating the dangling "spec 048" citation.

10. **`techContext.md`** — old: "WMO (V14/V17) | `WmoDetailReader`, `WmoSummaryReader` | Complete". New: `WmoDetailReader` does not exist. `WmoSummaryReader` is correct; the detail-reading surface is actually ~15 specialized readers under `src/core/WowViewer.Core.IO/Wmo/` (e.g. `WmoGroupMeshDetailReader`, `WmoMaterialDetailReader`, `WmoLightDetailReader`, `WmoPortalDetailReader`, `WmoDoodadDetailReader`), aggregated via `WmoRenderDocumentReader.cs`.

11. **`techContext.md`** — old: "DBC/DB2 | `DbcLookup`, `DbClientFileReader` | Complete". New: `DbcLookup` does not exist anywhere in `src/`. `DbClientFileReader` is correct; the DBC surface is actually `DbcReader.cs`, `DbcTableLoader.cs`, `ArchiveReaderDbcProvider.cs`, `RosettaDbcGenerator.cs`, plus table-specific readers, all under `src/core/WowViewer.Core.IO/Dbc/`.

12. **`techContext.md`** — old: External Dependencies list omits several vendored submodules. New: add `TACTSharp` (`libs/wowdev/TACTSharp`, CASC/TACT client-data access, referenced by `WowViewer.Core.IO.csproj`), `WoWDBDefs` (`libs/wowdev/WoWDBDefs`, DBC schema defs bundled with the viewer build), `wow-listfile` (`libs/wowdev/wow-listfile`), and `WoWTools.Minimaps` (`libs/Marlamin/WoWTools.Minimaps`). Evidence: `.gitmodules` (repo root) and the referencing `.csproj` files.

13. **`techContext.md`** — old: "GillijimProject: Alpha WDT/ADT helpers (libs/ submodule)". New: the wow-viewer copy at `libs/WoW-Tools/GillijimProject/` is **not** a registered git submodule — `.gitmodules` only registers gillijimproject under unrelated trees (`PM4Tool/docs/apps/gillijimproject`, `parpToolbox/...`). It is vendored/copied source, not a submodule, as currently checked in under `wow-viewer/libs/`.

14. **`coding_standards.md`** — old: Project Layout tree under `src/core/` lists only `Core`, `Core.IO`, `Core.PM4`, `Core.Runtime`, `Core.Anim`. New: same additions as correction #7 (`Core.Curation`, `Core.Editor`, `Core.Renderer`) plus `src/tools-shared/` as a sibling top-level folder alongside `src/core/` and `src/viewer/`.

15. **`data-paths.md`** — old: Quick Reference table presents `WOWVIEWER_WORKSPACE`, `WOWVIEWER_CACHE`, `WOWVIEWER_DATASETS`, `WOWVIEWER_TMP`, `WOWVIEWER_TEST_DATA`, `WOWVIEWER_STAGED_CLIENTS`, `WOWARCHIVE_MOUNT` as working overrides, and "How Overrides Resolve" describes a CLI-arg → env-var → default resolution chain. New: none of these seven env var names appear anywhere in `src/**/*.cs`, `tools/**/*.cs`, or `data-harvester/`. The only env vars actually read in the C# codebase are `WOWVIEWER_PM4_PROFILE` (undocumented) and several `PARP_*` debug flags (`PARP_MDX_TEXTURE_DIAG`, `PARP_M2_ENABLE_ANIMATION`, `PARP_M2_FORCE_SOLID`, `PARP_MDX_DEBUG`). Concretely, `Pm4OverlayCacheService.cs:54` resolves its cache root from `AppDomain.CurrentDomain.BaseDirectory`, not from `WOWVIEWER_CACHE` or a workspace-root convention. Either the override mechanism needs to be implemented, or the doc needs to state plainly that these are aspirational/planned rather than live.
