# Speckit Cleanup Audit — 2026-09-10 (first real 224-T201 run)

## Scope and status

This is the **first actual receipt/symbol audit** the Spec 224 governance skill defines (224-T201).
The 2026-09-06 pass (see `cleanup-2026-09-06.md`) explicitly left this unperformed — it only
reorganized files and routing. This pass audits every checked task in every active spec listed in
`specs/STATUS.md`'s weekly order.

**Partial run — 3 of 5 specs with checked tasks were not audited.** Parallel read-only agents were
launched for the 5 active specs carrying checked tasks (233, 231, 227, 223, 232). Two completed
(227, 232); three (233, 231, 223) failed on a session API rate limit (`resets 6pm America/New_York`)
before producing a result. **Do not treat 233's, 231's, or 223's checked tasks as verified by this
pass** — they were checked/unchecked by whatever state they were already in, not audited here.
224-T201 and 224-T202 remain open (`[ ]`) in `tasks.md`; this report is progress toward them, not
their closure.

## Specs audited

### 227-ui-reaudit — PASS, no changes

Both checked tasks (T001, T002) have real receipts: files-changed lists, real verification
commands with exit codes, and criterion-to-evidence tables that honestly mark unmet criteria as
"Not met" rather than overclaiming. Independently re-ran the symbol search T001's receipt cites
(`rg` for 6 named methods) and confirmed real hits in live source, not phantom.

**T004 (the gate blocking Spec 228) confirmed genuinely open**, not stale: exact text is
`- [ ] T004 [US1] Re-check the inventory gate and record the criterion-to-evidence receipt...`,
no `evidence/t004-inventory-gate.md` exists, and the spec's own documents (tasks.md Phase 1
checkpoint, `surface-inventory-v2.md`) independently agree the gate is open. Spec 228 staying
blocked is correct.

### 232-cartography-composition-project — 8 of 12 checked tasks corrected to `[ ]`

| Task | Before | After | Reason |
|---|---|---|---|
| T015a | `[x]` | `[x]` | PASS — real unit test, symbols confirmed. |
| T015b | `[x]` | `[x]` | PASS — deterministic direction-consistency test. |
| T015c | `[x]` | `[x]` | PASS — 4/4 criterion table backed by 33/33 focused tests + symbols confirmed. |
| T015e | `[x]` | `[x]` | PASS — concrete measured evidence, symbol `McalAlphaPackFull` confirmed. |
| T050 | `[x]` | `[ ]` | Receipt marks the minimap-visual criterion "Pass" from source + build alone; no operator visual evidence (AGENTS.md §6). |
| T052 | `[x]` | `[ ]` | No `evidence/` receipt exists at all — inline commit reference only, no verification commands or criterion table. |
| T056 | `[x]` | `[ ]` | Cited receipt filename doesn't exist on disk (phantom link); real combined receipt marks the actual criterion "Open, operator-owned." |
| T057 | `[x]` | `[ ]` | Task's own text already said "Unchecked pending..." while the box read `[x]` — literal self-contradiction. |
| T058 | `[x]` | `[ ]` | Task's own text: "interactive witness operator-owned" — self-contradiction. |
| T059 | `[x]` | `[ ]` | Same self-contradiction pattern as T058. |
| T064 | `[x]` | `[ ]` | Task's own text: "Visual witness operator-owned"; receipt's decisive criterion marked "Open, operator-owned." |
| T066 | `[x]` | `[ ]` | Task's own text: "Visual witness operator-owned"; receipt marks BOTH acceptance criteria "Open, operator-owned." |

None of the un-checked items are claimed wrong in substance — several (T015-series siblings, the
underlying code for T050/T056-066) look like real, working implementations. The defect is purely
in receipt/checkbox discipline: checking a box before the acceptance criteria it names (usually an
operator visual/interactive witness) actually has evidence.

## Specs not audited this pass (retry needed)

- **233-marketing-capture-automation** (14 checked tasks) — agent failed on rate limit before any
  findings; a partial transcript shows it was mid-way verifying chrome-hide/restore wiring for
  T012/T013 when it was cut off.
- **231-editor-archaeology-ui-overhaul** (23 checked tasks) — agent failed on rate limit before any
  findings; a partial transcript shows it was mid-way reading evidence files.
- **223-ui-consolidation-audit** (25 checked tasks) — agent failed on rate limit before starting
  substantive output. This is the highest-priority re-audit target: it is the oldest/largest set of
  checked tasks and other memory notes already flag an outstanding "operator gate retest (fog, WMO,
  playback/capture, ffmpeg, spatial UI)" that a receipt-discipline audit is exactly positioned to
  catch if any of those areas were checked on build/test proof alone.

## Memory-bank compression

- `memory-bank/activeContext.md`: 188 → ~65 lines. The removed content was almost entirely
  session-by-session "landed X, receipt Y" narrative that already duplicated `progress.md`'s dated
  entries verbatim — a dashboard file had drifted into being a second progress ledger. Full prior
  version preserved at `memory-bank/archive/2026-09-10-pre-cleanup-active-context.md`, indexed in
  `archive/README.md`. This is the second time in 4 days this file needed compressing (last done
  2026-09-06 to 48 lines) — the recurrence itself is worth naming: nothing currently stops a
  session from appending narrative here instead of to `progress.md`.
- `memory-bank/progress.md`: left as-is (216 → 216+ lines) — all entries are within the current
  month, which is what the skill's archival rule keys on. Not touched beyond appending today's
  entry.

## New operator-reported items filed (not investigated, not fixed)

Operator directive (2026-09-10, verbatim intent preserved) surfaced 6 real gaps unrelated to the
governance audit itself. Filed into the specs that already own the relevant surface, per AGENTS.md
§9.1 (operator-originated wording may enter a spec directly):

- Spec 232 Phase 7 (new): T067 (remove "Bake MCSH shadows" minimap option — permanently, per prior
  measurement that MCSH isn't encoded in minimaps at all), T068 ("Include WMO geometry" minimap
  checkbox doesn't work), T069 (no-water minimap shading glitches vs normal), T070 (cell fine-tune
  needs true 1-cell X/Y granularity — concrete failing case: Hellfire Ramparts vs Expansion01, off
  by 1–3/1–2 cells), T071 (offset counters too small to show a signed two-digit value, must scale
  with UI text size).
- Spec 231 Phase 8 (new): T080 (hovered-WMO doodad-set combo disappears on mouse-leave, unusable).
- Operator also re-stated that the missing Archaeology/Editor save path ("the whole point of this
  whole toolkit") is still the top gap — this is exactly Spec 234 (current branch), still Draft
  and not yet planned. No new task needed; it's already the entire spec.

## Criterion-to-evidence

| Cleanup criterion | Evidence | Result |
|---|---|---|
| Every active spec's checked task audited mechanically | 227, 232 audited; 233/231/223 failed on rate limit | **Partial — retry 233/231/223 owed.** |
| Un-check any checked task lacking a real receipt | 8 corrected in 232, 0 needed in 227 | Met for the 2 audited specs. |
| Archive closed specs | No spec was fully closed (all still have real open work) | N/A this pass. |
| Compress memory banks | `activeContext.md` 188→~65 lines, archived not destroyed | Met. |
| Update the AGENTS.md cleanup ledger | See ledger entry dated 2026-09-10 | Met (marked partial). |
| Operator confirms STATUS/activeContext reflect reality | Not sought this pass | **Open — Gate 2 remains unchecked.** |

## Follow-up boundary

Re-run the 233/231/223 audits (same read-only-agent procedure) after the rate limit resets, apply
any corrections the same way as 232's, then close 224-T201 for real (all 5 specs audited) before
touching 224-T202 (archival) or Gate 2 (operator confirmation).
