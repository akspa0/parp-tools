# Contracts

This convergence feature does not define public network APIs.

The contracts in this folder exist to keep the planning surface explicit and machine-checkable:

- `renderer-capability-slice.schema.json` describes a mapped renderer capability inherited from specs 030-032
- `renderer-validation-scenario.schema.json` describes a staged-client validation case used for convergence signoff
