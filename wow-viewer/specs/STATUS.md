# Spec Status Router

**Implementation order: [NEXT-DAY-PLAN.md](./NEXT-DAY-PLAN.md).** That file is the ordered pass through the open specs and the reasoning for the order; this file is the per-spec detail.

This is the current routing index for `wow-viewer`. It is intentionally short. The owning spec and
task file are authoritative; this file only tells a new agent where to start.

## Start here

| Priority | Spec | State | Next bounded action |
|---|---|---|---|
| 1 | [214 5.0.1 Physics - decode data, drive a licensed solver](214-mop-physics-domino/spec.md) | **Draft: spec + quality checklist complete 2026-09-02; not planned** | Grounded against the LIVE 5.0.1 binary, not from memory. The solver is **Domino** (`Engine\Source\Domino/{Common,Dynamics,Collision}`); WoW's adapter is the sibling `Engine\Source\Physics/PhysData.h` + `Physics.cpp` `0x00d7592c` / `PhysicsInt.cpp` `0x00d75a40` / `PhysData.cpp` `0x00d75dac` -- two layers, do not conflate. **Entry point: `"Domino Assertion: %s, in file %s, line %d"` `0x00e0ac8c` -> handler `FUN_00c29680`; its ~90 callers each pass their own file+line, so decompiling a caller yields the originating header and line for free.** Contiguous region `0x00c26e50`-`0x00c4e012` (~160KB). **~90 is a FLOOR, not the subsystem size** -- only asserting functions are visible to the pivot. **Operator direction 2026-09-02: use an existing permissively licensed C# physics library; NO copyrighted engine code in the tooling.** Domino is decoded as a CONTRACT (data layouts + observable behaviour + budget rules) and is never reproduced - transcribing its algorithms would make this a derivative of Blizzard's engine. Candidates (selection + license verification are `plan.md` deliverables, not settled here): BepuPhysics v2 / Jitter2 (pure C#), PhysX / Bullet (native + bindings). Cloth capability must be a SELECTION criterion, not a discovery - US4 is the visible flag motion. Client culls physics by distance (`"Physics culling dist set to %f."`) -- simulating everything is not faithful and compounds the known per-object frame cost. **Next: speckit-plan**; sequence US1-US4 (delivers the visible flag motion) before US5-US7. Evidence: [workstream-atmosphere-501-ghidra.md](../memory-bank/workstream-atmosphere-501-ghidra.md). |
| 1 | [215 5.0.1 Weather System](215-mop-weather-system/spec.md) | **Draft: spec + quality checklist complete 2026-09-02; not planned** | Anchors measured: `MapWeather.cpp` `0x00debaae`, `Weather.dbc` `0x00e059f0`, `Lightning.cpp` `0x00e26da8`, `EffectSwirlingFog.cpp` `0x00e0ff54`, `ParticleSystem2.cpp` `0x00d74f84`. **This spec does NOT own lighting/fog/sky** -- 143, 147 and 160 do (160 alone is 72 tasks / 8 phases). FR-015 forbids a parallel lighting or fog model; weather *drives* the owning systems through declared interfaces, and SC-006 measures that zero extra models were added. Wind is exposed as its own interface (FR-013/014, defined neutral value, no consumer required) -- that is the single join with 214, so neither spec blocks the other. **Next: speckit-plan.** Open: `EffectSwirlingFog` ownership is genuinely ambiguous, settle with 147. |
| 1 | [216 Model Cursor as a Scene Light Source](216-model-cursor-light-source/spec.md) | **Draft: spec + quality checklist complete 2026-09-02; not planned** | Any MDX/M2 as the cursor, with its particles and **its lights illuminating the scene**. Target: reproduce the 2001 thief-with-torch screenshot - `areatest.lit`, ~3am, torch as the only light. **Measured before writing: `areatest.lit` is ALREADY probed by `LitSourcePathResolver`; particles ALREADY render via `ParticleRenderer`; MDX lights ARE parsed and used by `UploadMdxLights` - but that uploads a model's lights as uniforms into THAT MODEL'S OWN shader program (cap `MaxMdxLocalLights = 8`), so a torch lights only itself.** That single fact is the whole feature: without it this is a day of wiring that yields a torch glowing alone in an unchanged dark room. Real work is scene-wide model lighting across the terrain/WMO/model render paths. **Operator correction 2026-09-02: the light comes from the EFFECT, not the `LITE` chunk.** A torch's illumination is its FIRE - a particle system. Effects must cast light (FR-008/008a/008b), `LITE` lights are an additional source (FR-008c). `MdxParticleEmitter2` already carries position, an animated colour ramp and alpha/scale/emission-rate, so a derived light **flickers because the flame flickers** with nothing authored separately (SC-002a measures that as a correlation, not a visual assertion). **Historical framing that reframes the whole feature: the lights were always in the data - `areatest.lit` carries authored point lights - and 2001 hardware could not afford one well-done point light in a dark scene.** Hence one screenshot, then ~a decade of absence. So this is running lighting the data has always described, and a failure to reproduce it points at OUR lighting model, never at missing data. FR-015a: the profile's point lights must render as real lights, not an ambient tint, or there is no dark scene to light - **ownership shared with 143, settle in planning, do not grow a second lighting path**. Era dungeon screenshots (Deadmines) show the same thing from PLACED objects, so FR-014a/SC-010 require the mechanism to be designed to drive any model instance; the cursor is just the cheapest test rig. Enabling it world-wide is out of scope (cost question unanswered) but foreclosing it is not allowed. UNMEASURED: whether 5.0.1 derives light from effects this way - `ParticleSystem2.cpp` / `M2Light.cpp` are the anchors. **Next: speckit-plan.** |
| 1 | [217 Audio Playback Lifecycle](217-audio-lifecycle/spec.md) | **Draft: spec + quality checklist complete 2026-09-02; not planned** | Operator: one-shots fire forever, music does not play, so audio is disabled by default. **The 5.0.1 binary explains it: a playing sound is a state machine over SIX explicit lists** (`WaitingForDownload` / `Loading` / `GoGoGo` / playing / `FadeList` / `DeleteList`, reclaimed by `ProcessSoundKitObjectDeleteList`). **A sound that never reaches the delete list never stops - this is a LIFECYCLE defect, not a decoding one**, which is why attention to the decoders never fixed it. Three more findings: there are **three** repeat modes not two (`"Periodic sound between tweets"` - repeats WITH GAPS, prime suspect for the reported noise); `PlaySoundKitID(ID, ["SFX"/"Music"/"Ambience"/"Master"], forceNoDuplicates)` gives category buses + duplicate suppression; channels are finite and prioritised, and exhaustion routes to DELETION as a handled outcome. Backend is **era-split**: 0.5.3 DirectSound/DirectMusic+MIDI/DLS, 5.0.1 FMOD - the discipline transfers, the backend does not. **Autoplay is the OUTCOME (FR-016), gated on US1/US2/US4 being demonstrated.** Next: speckit-plan. Evidence: [workstream-audio-501-ghidra.md](../memory-bank/workstream-audio-501-ghidra.md). |
| 1 | [212 3D Spatial UI Shell](212-spatial-ui-shell/spec.md) | **Draft: spec + quality checklist complete 2026-09-02 (US6/US7/US8 added same day); not planned** | Operator-requested direction: panels stop being 2D windows carved out of the viewport (`TryGetSceneViewportRect`) and become interactive surfaces composited over a full-window scene, mounted on a rig whose profile follows the workspace task chosen in the top bar. Generalises spec 210's OpenSCAD asset path (`OffGeometry` / `ProceduralMeshLoader`, committed `.scad` + `.off`). **Next: speckit-plan.** Ordering is load-bearing — US1 pointer-to-content accuracy must fully pass on flat surfaces before US4 curved shells are attempted; `plan.md` must not reorder. Two open design decisions for the plan: what screen-anchored constructs (modals, menu dropdowns, path picker) do in a spatial shell (FR-022), and how surface hit-testing / profile resolution live in `WowViewer.Core*` given no test project references the viewer. **Three operator-driven stories added 2026-09-02, all independently shippable and deliberately NOT gated behind the shell work: US6 selection outlines (highlights trace the object's silhouette, not a box - boxes are larger than small objects), US7 the museum profile (camera-locked floating HUD, panels/tabs/readouts hidden, "more museum than in-your-face data explorer"), US8 3D tool controls (time-of-day becomes an interactive clock face; ImGui has no circular control so it must be drawn + hit-tested).** FR-032 is load-bearing: a HUD element must invoke the SAME underlying action as its full-shell equivalent - two implementations of one operation drift apart and the profile silently becomes a fork. |
| 1 | [213 MCP Tooling Harness](213-mcp-tooling-harness/spec.md) | **Draft: spec + quality checklist complete 2026-09-02; not planned** | Model Context Protocol **server** over the ten CLI tool projects (`inspect`, `harvest`, `capture`, `converter`, `enrich`, `mask-validate`, `validation-capture`, `wdl-read`, `wmo-minimap`), so an external orchestration/inference harness can discover and drive them with no hard-coded argument lines. Client is deferred (US6, P4). **Next: speckit-plan.** The load-bearing requirement is FR-007: MCP schema and CLI parser derived from **one shared definition**, with the build failing on divergence — this is the mechanism that prevents the documented CLI-drift defect rather than reproducing it; a hand-maintained parallel schema does not satisfy it. Plan must pin the spec version + transport and enumerate per-tool result shaping tool by tool (the largest unestimated part). |
| 1 | [210 3D Scene Cursor & In-World Spatial Selection](210-3d-scene-cursor-selection/spec.md) | **Phases 1–4 Implemented & Unit Tested 2026-09-02; Ready for Operator Interactive Verification** | OpenSCAD MCP server configured and verified. ProceduralMeshLoader & OffGeometry parser implemented with full unit tests (3/3 green). SceneCursorRenderer implements authentic WoW gauntlet (`Interface\Cursor\Cursor.mdx`), procedural pointer, and 3D reticle. Camera culling invariant strictly enforced (near-plane clamp $Z \ge Z_{\text{near}} + 0.15\text{ yd}$, depth range bias `0.0..0.05`, distance-proportional scaling). Seamless ImGui interop via `CanSceneConsumeMouse` and `ImGui.SetMouseCursor(None)` hides OS cursor only over 3D viewport, instantly restoring it over ImGui chrome/modals. SceneClusterSelector3D replaces intrusive 2D modal menus with in-world 3D orbital rings and interactive candidate markers. User-selectable cursor style and scale dropdown added to Settings > Interface. |
| 1 | [211 WMO Interior Ray Picking, Doodad Selection & Ghost Transparent Wireframes](211-wmo-interior-picking-ghost-wireframe/spec.md) | **Phases 1–3 Implemented & Unit Tested 2026-09-02; Ready for Operator Interactive Verification** | WMO bounding box container fall-through implemented (`WmoContainerFallThroughFilter`, 6 unit tests green); interactive WMO doodad ray picking (`ObjectType.WmoDoodad`, `TryPickDoodadsByRay`); dual-pass ghost transparent wireframe rendering (33% alpha fill + prominent line overlay) landed for M2/MDX (`ModelRenderer.cs`), WMOs (`WmoRenderer.cs`), and terrain (`TerrainRenderer.cs`). Inspector and framing support wired in `ViewerApp.cs`. See [tasks.md](211-wmo-interior-picking-ghost-wireframe/tasks.md) |
| 1 | [197 UI Workspace Profiles, Editor Mode, PM4 Mouse Inspection, Multi-Client Staging & MoP ADT Pipeline](197-workspace-profiles-editor-and-mop-adt-pipeline/spec.md) | **Active: split reader/runtime + conversion target boundary implemented; native evidence, merger, and MoP writer open** | Explicit target selection now separates Alpha 0.5.3 WDT, LK v18 monolithic ADT, and unavailable native MoP split output. `_obj0`/`_obj1` + `_tex0`/`_tex1` discovery/loading, headerless companion MCNK routing, sparse-slot preservation, and focused tests are landed. Next: audit compact consumers, extend band-1 merger/transfer conversion, then implement a slot-aware native split writer. See [tasks.md](197-workspace-profiles-editor-and-mop-adt-pipeline/tasks.md) |
| 1 | [205 MH2O LiquidObject vertex format](205-mh2o-liquid-object-vertex-format/spec.md) | **IMPLEMENTED 2026-09-01; operator visual proof owed** | Phase 1 gate PASSED and **corrected two research assumptions**. (1) The wiki's "values >= 42 are LiquidObject ids" threshold is **WRONG for this client**: `LiquidObject` real ids run **57..2390** and **42 is absent**, so ocean must stay unresolved -- which keeps the flat plane, and flat is CORRECT for ocean. (2) The first gate run reported the exact inverse because **`DBCDRow.ID` is a POSITIONAL key for these WDB2 tables**, not the row id (`storage[42].ID == 316`); keying on `row.ID` silently resolves the wrong row for every sparse id. Fixed via `DbcTableLoader.ResolveRowId`. **Verified resolution**: the 144 river layers `2325/2333/2372 -> LiquidType 5 -> LiquidMaterial 1 -> LVF 0 (HeightDepth)`, with the independent byte-plausibility probe **agreeing**. Also measured: this client's `LiquidMaterial` only ever yields LVF 0 or 1. **Shipped**: `LiquidVertexFormatChain` + `DbcLiquidObjectTable` + `DbcLiquidMaterialTable` (Core.IO/Dbc); **both** decoders (`Mh2oChunk.Parse` render path, `AdtLiquidReader.ParseLayer` harvest path) now resolve the field and gate their vertex-block switch on it; `StandardTerrainAdapter` loads the chain and reports each unresolved value once. 15 new tests incl. a **two-decoder parity test**; full Core.Tests **1282 passed / 9 pre-existing failures** (identical set). **Next: operator loads `HawaiiMainLand` and confirms the waterways slope** and the chunk-boundary steps are gone. See [evidence/phase1-dbc-chain-verified.md](205-mh2o-liquid-object-vertex-format/evidence/phase1-dbc-chain-verified.md) |
| 1 | [208 Cross-map tile transplant (Pre-Alpha Restoration)](208-cross-map-tile-transplant/spec.md) | **Phase 0 Complete (audit & channel reconciliation done); Phase 1 next** | Phase 0 audit completed: mapped map identity to source chunkReader and target MCLY texture remapping boundaries; confirmed 195 fails to rotate normals, placement rotations, and relative positions (defect in 195 to fix); reconciled `ChunkTranspositionOptions` directly onto `PhaseDataChannel` (Constitution II); confirmed chunk-granular (1/16 tile) lattice granularity preserves raw vertex fidelity. See [evidence/phase0-reconciliation.md](208-cross-map-tile-transplant/evidence/phase0-reconciliation.md) and [tasks.md](208-cross-map-tile-transplant/tasks.md) |
| 2 | [209 WLW/MCLQ liquid convergence](209-wlw-mclq-convergence/spec.md) | **Phase 1 Complete (diagnosed & measured 2026-09-02); Mechanism B confirmed as shoreline gap cause** | `inspect adt liquid-convergence` shipped and run against 0.5.3 Azeroth (500 liquid tiles, 108 with WL* from `misc.mpq`). **Union invariant verified (SC-002)**: exactly 0 cells covered by a source were dropped in unified liquid. **Mechanism A (MCLQ sag)**: fixed via `LiquidSurfaceInterpolation`. **Mechanism B (waterline culling) CONFIRMED**: 585,108 WL* cells culled by terrain across Azeroth; **459,374 of those culled cells had NO MCLQ coverage**, creating empty shoreline strips (e.g. 23,192 cells on Wetlands `Azeroth_31_29` alone). Where sources overlap in open water, surface heights agree almost identically (mean $\Delta H = 0.00$). Shoreline gaps are caused by `KeepOnlyAboveTerrain` removing WL* where MCLQ has no coverage. See [evidence/phase1-convergence-measured.md](209-wlw-mclq-convergence/evidence/phase1-convergence-measured.md) and [tasks.md](209-wlw-mclq-convergence/tasks.md) |
| 1 | [207 Object draw-call reduction](207-object-draw-call-reduction/spec.md) | **Phase 1 implemented 2026-09-02; operator capture owed** | **Operator ablation decomposes the frame**: terrain-only >300 FPS (<3.3 ms), +WMOs 17 FPS (**WMOs ~55.5 ms**), +doodads 10 FPS (**doodads ~41.2 ms**) -- objects are **~97% of the frame**. **The cost is per-draw CPU overhead, derived not assumed**: 41.2 ms / 8,989 submitted MDX = **4.6 us each**; fill rate cannot produce a constant per-ITEM cost across subsystems with wildly different pixel coverage while terrain-only runs >300 FPS. Draw calls are the currency. Also note **`DeferredAssetLoads` max is now 4.1 ms** against spec 204's 442.9 ms baseline -- 204 is not this scene's bottleneck -- and the distribution is **flat** (p99/median 1.7, 2025/2048 frames over budget), so this is throughput, not hitching. **Three defects found by source inspection**: (1) faded instances were added to `VisibleMdx` unconditionally -- full draw call, zero pixels (**fixed**, counted in `FullyFadedMdxCount`, suppressed under capture overrides after the existing test caught the regression); (2) the instancing path admitted only `OpaqueFade >= 0.999` at **two** gates, forcing the entire fade band -- **36% of the visible disc** (`1-0.8^2`) and exactly the population instancing helps most -- onto one-draw-per-instance, while per-instance fade was **already plumbed end to end** (`GpuInstanceData` -> attribute 10 divisor 1 -> `aInstanceFade` -> `finalAlpha`); (3) WMO groups admitted wholesale: **80 considered, 80 admitted, 0 rejected**, 62.5% conservative fallback, portal traversal **0**. **Phase 1 shipped**: the gate was *split*, not deleted -- one instanced draw shares one blend state, so faded geometry in the opaque batch would render opaque. Two batches per model now (opaque: blend off/depth write on; faded: blend on/depth write off, drawn after), bounding draw calls at **2 per model instead of 1 per instance**. `QueueGpuInstance`'s **silent drop** removed (FR-004). Decision logic lives in Core (`GpuInstanceBatchPolicy`) because **no test project references the viewer**; 6 new tests, full suite **1326 passed / 9 pre-existing failures**. **Next: operator captures the fixed camera** -- expect opaque MDX draws >=10x lower (SC-001), `MdxOpaqueSubmission` median 10.66 -> <=3 ms (SC-002), and the new `of which distance-faded` row non-zero. Then Phase 2 (WMO group admission) and Phase 3 (skin-profile LOD). |
| 1 | [206 Zarr-first asset residency](206-zarr-first-residency/spec.md) | **Draft: spec + checklist complete** | Operator-requested 2026-09-01 after a MoP flight where **every recent hitch was `DeferredAssetLoads`** (59.6/131.8/136.8/160.3 ms at 3 FPS). Their stated cause was the MPQ read; **204 measured that the read is not the cost** — parse + adaptation + BLP decode + GL upload all run in-frame. The spec keeps the requested solution (Zarr) and re-aims it at the measured cause: store **already-decoded, render-ready** arrays so the decode stage is *deleted*, not relocated. **Complementary to 204, not a substitute** — 204 still owns moving the GPU upload off-thread. Member of the [Client Datastore epic](epic-client-datastore/epic.md); **builds no new store**, but inverts 183's viewer-last ordering. **The blocking gap is measured**: `ZarrTileDatasetLoader.LoadTile` throws `NotImplementedException`, `StoreIndexReader` states *"this codebase has no C# Zarr array reader"*, and the one C# writer (`RosettaDatastoreWriter`, spec 190) emits `codecs: [bytes]` — uncompressed — so no codec path is exercised while Python writes Blosc/lz4. Also retires the `output/cache/` **path shim**: `ViewerApp.cs:12398` writes byte-for-byte client copies to disk *"for parsers that expect file paths"* — **~1.75 GB today, incl. `Kalimdor.wdt` 1.03 GB**. That slice (US1) is independent and can ship alone. Scope decisions RESOLVED 2026-09-01: full renderer coverage, derived/rebuildable store, textures stored BOTH as portable pixels and a derived block-compressed array. See [spec.md](206-zarr-first-residency/spec.md) |
| 1 | [204 Off-thread asset decode](204-off-thread-asset-decode/spec.md) | **Planned: spec + research + plan + contracts + tasks complete; Phase 0 not started** | **The operator's premise was right**: it is not the SSD and not the MPQ reader. `MpqDataSource` already runs 2 prefetch workers and root bytes are usually warm — but `WorldAssetManager` contains **no threading at all**, so parse, adaptation, BLP decode and GL upload all run inside the frame. `DeferredLoadBudget` admits **one unbounded load per frame by design** (guaranteed progress) and its own docs name this exact fix as "Spec 153 Phase 5 step 2, deliberately not attempted here"; it counts the damage in `OversizedAdmissionCount`, which nothing read until 2026-09-01. Worse, the CPU throttle clamps to 1 load/frame on a slow frame while the first load stays unconditional — cutting streaming ~6x **without reducing the hitch**, a self-reinforcing spiral. Measured: `DeferredAssetLoads` owns 12 of 13 recent hitches at **26.4–68.1 ms**, median frame 75.26 ms, 2047/2048 frames over 33.3 ms. **Phase 1 is a hard gate** — GL objects live on static fields and a process-global log writer is re-opened per model from a constructor. See [spec.md](204-off-thread-asset-decode/spec.md) |
| 1 | [202 Unified model batching](202-unified-model-batching/spec.md) | **Phase 0 + Phase 3 instancing implemented 2026-09-01; one operator flight left (T305/T306)** | **Root cause found in source, no flight needed**: `SupportsGpuInstancedOpaque` was hardcoded `false` on `MdxRenderer` and delegated to it by `M2Renderer`, so **no renderer could ever instance** and the instanced count was provably zero — "batched" has only ever meant state-hoisted. The CPU side was complete (instance VBO, divisor-tagged attributes at locations 6–10, `DrawElementsInstanced`) but **the vertex shader declared only locations 0–5**, so enabling the flag as it stood would have stacked every doodad on the world origin. Shader completed, instancing enabled behind a runtime toggle with an automatic disable if it fails to compile. `MdxOpaqueBatchingEnabled` defaults `true`, so the recorded 100%-unbatched was never the toggle (R7). Models with local MDX lights are held out of instancing — their light pivots are transformed per instance and are not in the payload (R10, contract C2). | **Phase 0 landed**: instanced, state-hoisted, unbatched and unbatchable are now four separate numbers per render path; draw calls are counted at the four GL call sites (not inferred — a model draws once per geoset or section); every instance short of instancing carries the gate that stopped it; and `DistinctModelCount` records the per-model instancing floor. **T102 answered**: the status bar's `429` *is* a distinct-model count, but of models **resident**, not **visible**, so ~30x is a ceiling on the prize rather than the prize — and the `13180/20115` gap is culled placements, not lost ones. Rendering behaviour is unchanged by design. | **Two metric conflations found, not one**: the `batched` counter adds *GPU-instanced* (one draw per model) to *state-hoisted* (still one draw per instance), so a "batched" scene may issue exactly as many draws as an unbatched one. And **three** independent gates force unbatched — route, instancing support, and `OpaqueFade < 0.999`, which drops every distance-faded doodad. The batch key is the renderer object, so the per-model floor is the distinct-visible-model count — **measure it (T101) before building toward it**. The operator's stated #1 issue, affecting **all** eras. Measured: 13,180 opaque + 260 transparent draws, 100% unbatched, median 84.44 ms / 7 FPS. Root cause is architectural — batchability is a property each renderer opts into, so whether an instance batches depends on which loader constructed it. Absorbs 153 US3's legacy-MDX batching into one decision (Constitution II) rather than adding a fifth parallel mechanism. **Do not start before 201**: the counters conflate M2 and MDX, so any before/after is unreadable. See [spec.md](202-unified-model-batching/spec.md) |
| 1 | [203 Multi-phase map composition](203-multi-phase-map-composition/spec.md) | **IMPLEMENTED 2026-09-02; operator visual proof owed** | Both defects fixed. (1) `MergePhaseTile` was `parent.Chunks[i] = phaseChunk` -- a wholesale replacement preserving only `Liquid` -- so every base field the phase chunk did not carry was destroyed. Once split `_obj0`/`_tex0` companions landed a phase tile became a PARTIAL chunk, and a partial chunk replacing a complete one is the missing-trees and blank-plate symptom. Now a **per-channel patch**: `PhaseChunkMerger.Merge` composes field by field. (2) Placements were appended unconditionally, so a restaging phase produced the old set AND the new one; now **presence-gated replace** -- a phase that ships placements owns them for that tile, one that ships none leaves the base map's alone (`PhaseCompositionPolicy.PhaseOwnsPlacements`). **Multi-phase**: `ITerrainAdapter.OverlayMapName` (single string) is replaced by `IList<PhaseLayerSettings> PhaseLayers`, applied in order, with `OverlayMapName` kept as a shim over the first enabled layer so existing callers and saved settings keep working. **Blank-plate guard**: `OnlyTakeWhatThePhaseCarries` (default ON) takes a channel only where the phase really has it; turning it OFF makes the phase authoritative even where empty, which is how a phase deliberately CLEARS something. **UI**: the single-overlay control is replaced by a Photoshop-style **Phase Map Layers** panel (`ViewerApp_PhaseLayers.cs`) -- add/remove/reorder/enable layers, ten per-channel checkboxes each (heightmap, normals, holes, texturing, MCCV, MCSH, liquid, doodads, WMOs, AreaID) plus All/None/Objects-only/Terrain-only presets. Policy lives in Core (`PhaseComposition.cs`) so it is unit-tested: **14 new tests**, full Core.Tests **1296 passed / 9 pre-existing failures**. **T001 ANSWERED 2026-09-02 and phase discovery is now DBC-driven**: `Map.dbc` in 5.0.1.15464 ends with `ParentMapID<32>`; **18 of 239 rows carry a parent** (`-1` = none), e.g. `JadeForestAllianceHubPhase`/`JadeForestBattlefieldPhase` -> `HawaiiMainLand` (Pandaria), the three Gilneas maps -> `Gilneas2`. **`Phase.dbc` CANNOT do this**: its 5.0.1 layout is `ID, Name, Flags` with **no map column** -- the `MapID` columns in the docs belong to 4.0.0.11927-12539 and were removed by 4.0.0.12911, so the 4.x era briefly had *more* map info here, inverting the expectation. Phase.dbc contributes names only (814 rows, many literally "... Terrain Swap"), and **flag 4 is not a terrain-swap flag** (wrong in both directions: phase 106 is a swap with flags 0, phase 52 is flags 4 and not a swap) so the name is matched and the flag exposed raw. `PhaseXPhaseGroup` (1183 rows) groups phase ids, still with no map link. Shipped `DbcMapPhaseTable`/`DbcPhaseTable` + a general `inspect dbc dump` command; the layers panel now lists the current map's children with Add / Add-all. 13 more tests (1309 passed / 9 pre-existing failures). **Next: operator stacks two phases on Jade Forest and confirms base trees survive.** |
| 1 | [201 Render-path metric attribution](201-render-path-metric-attribution/spec.md) | **Phase 1 implemented 2026-09-01 (T001–T004); T005/T006 are operator flights** | Landed jointly with 202 Phase 0 — the same submission callbacks carry both decompositions, so splitting them would have meant instrumenting the hot path twice. FR-005's sum check is a unit test. Phase 2 (native-route M2 batching) stays gated on T005: if native-route M2 turns out to be a small share, *not doing it* is the correct outcome. | The panel's `MDX opaque: 13180 unbatched` counter aggregates **two different render paths** — `ResolveVisibleMdxRenderer` returns either `M2Renderer` or `MdxRenderer` and both increment the same field. `M2RouteType` already distinguishes five routes and is unused here. What the single bucket hides: an M2 on the **native runtime route** has no inner legacy renderer, so `RequiresUnbatchedWorldRender` is unconditionally true and `SupportsGpuInstancedOpaque` unconditionally false — those draws are **structurally unbatchable** and spec 153 US3's toggle cannot touch them. The code comment says as much: *"keep it isolated until a backend-specific batch key exists"*. Do this before any before/after flight on 153/198/200. See [tasks.md](201-render-path-metric-attribution/tasks.md) |
| 1 | [200 WMO portal admission — conservative fallback](200-wmo-portal-admission-fallback/spec.md) | **Planned: spec + tasks complete, Phase 1 not started** | Spec 151's instrumentation produced its first real reading (operator flight, Wandering Isle, 5.0.1): **145/145 placements and 806/806 groups admitted, zero rejected**. Portal culling runs and reduces nothing. Conservative fallback fired on **119 of 290 evaluations** (38.8% of admissions), GPU-instanced shell another 38.3%; `portal traversal only` scored **0**. The evaluator already names five fallback reasons but the panel shows only the first — Phase 1 counts them per reason and per WMO, and splits "no portal data in the file" from "portal data present, no graph built", which have opposite fixes. **Diagnose before changing culling logic.** Keep MDX batching off during comparison flights or the result is unreadable. Takes the admission slice off 151 so it can finish without 151's game-mode work. See [tasks.md](200-wmo-portal-admission-fallback/tasks.md) |
| 1 | [199 MCAL alpha decode correctness](199-mcal-decode-correctness/spec.md) | **Planned: spec + plan + tasks complete, Phase 1 not started** | **Four independent MCAL decoders exist** (`Lk/Mcal.cs`, `Maps/AdtMcalDecoder.cs`, `StandardTerrainAdapter.DecodeLayerBySpan`, `Vlm/AlphaMapService`) — a direct Constitution II violation. The harvest path hardcodes `bigAlpha:false` while the renderer reads it from MPHD, so harvested and rendered alpha disagree on every big-alpha map. `SynthesizeCataclysm400ResidualAlpha` **fabricates** a 64x64 map (`255 - sum(others)`) for any layer that fails to decode, which renders as the opaque chunk-sized blocks seen on Mogu'shan Palace. Two decoders select the encoding by **inferring spans** from the next layer's offset, which cannot be correct in general. Next: **T001-T005** — instrument the CURRENT behaviour and sweep the client library, so the failure rate is a number before anything is consolidated. Do not delete the fabrication first: the blocks are the only visible signal that decode fails. See [tasks.md](199-mcal-decode-correctness/tasks.md) |
| 1 | [198 M2/WMO shader permutation system](198-m2-shader-permutations/spec.md) | **Planned: spec + plan + contracts complete, Phase 1 not started** | The client selects a shader PAIR per batch by ordinal into two tables measured at `0x00eb4b10` (35 pixel entries, decoded in full) and `0x00eb4ba0` (16 vertex entries). Our renderers have no permutation system at all — one program, materials flattened to Opaque/Cutout/Blended. **The per-batch ordinal's source is NOT yet measured** and is gate T002; Phase 1 (enums, registry, fallback, report) is independent of it and ships with everything falling back, so the era gate and off-switch are exercised from the start. Do not assign a permutation to any batch before T002 resolves — FR-002 exists because MCXH and Ck24Type were both invented from unmeasured fields. See [plan.md](198-m2-shader-permutations/plan.md) |
| 1 | [196 WDL lattice magnetization & neighbor-mesh auto-fitting stratigraphy](196-wdl-lattice-magnetization-stratigraphy/spec.md) | **Complete: All 5 phases implemented & verified, 100% green tests** | Next-generation stratigraphy reconstruction with polarity inversion (negative scaling for Dragon Isles), multi-anchor datum selection (floor/ceiling/mean/custom), neighbor mesh auto-fitting within 1–3 chunk radii (minimizing boundary RMSE), WDL macro-lattice magnetization, 0-hitch background async mesh restoration, and companion modified WDL export. See [tasks.md](196-wdl-lattice-magnetization-stratigraphy/tasks.md) |
| 1 | [195 Overhead chunk manipulator & multi-tile sub-cell transposition](195-overhead-chunk-manipulator/spec.md) | **Complete: All 4 phases implemented & verified, 100% green tests** | Unified global chunk coordinate space ($1024 \times 1024$), multi-tile / sub-cell selection region, interactive 2D overhead canvas with tile and chunk grid lines, MCNK multi-attribute chunk transposition (MCVT heights, MCNR normals, MCLY/MCAL texture harmonization, hole masks, MDDF/MODF placements), in-viewport selection canvas, and full `EditorSession` undo/redo. See [tasks.md](195-overhead-chunk-manipulator/tasks.md) |
| 1 | [194 Temporal stratigraphy & weak signal dev mesh restoration](194-temporal-stratigraphy-weak-signals/spec.md) | **Complete: All 4 phases implemented & verified, 100% green tests** | Temporal stratigraphy scanner, level-count profiling (`surviving_height_levels`), seam discontinuity analysis, SIMD zero-allocation in-place mesh restoration, false-color stratigraphy in Viewer workbench (Archeology > Stratigraphy & Terrain Lab), in-app restored ADT exporter, and dual-era CLI batch scanner/patcher (`terrain-stratigraphy-scan`/`patch`). See [tasks.md](194-temporal-stratigraphy-weak-signals/tasks.md) |
| 1 | [193 Benilla 1.12.1 client reference & 1.x M2 parity](193-benilla-112-client-reference/spec.md) | **Active: Reference Architecture & Oracle Pack Authored** | Modern clean-room Rust 1.12.1 client reference (`samwhosung/benilla`) to cross-reference 1.x era M2 model parsing (`MD20 0x100`), embedded skin profile extraction (`nViews`/`ofsViews`), material texture unit blend modes, forward kinematics, and modern GPU draw batching inside our native C# tooling. See [tasks.md](193-benilla-112-client-reference/tasks.md) |
| 1 | [192 Terrain template brush & paste library](192-terrain-template-brush-generator/spec.md) | **Complete: All 6 phases implemented & verified, 100% green tests** | Terrain Template Brush & Paste Library with Interactive In-Viewer Map Generator. Mines recurring terrain height and multi-layer texture pastes from real ADT data, provides an interactive ImGui brush stamping plugin in the Viewer editor, and synthesizes templated procedural maps with authentic multi-tileset blending, road networks, and guaranteed $\le 25^\circ$ walkability. Unit tests (14/14 green) + CLI `terrain-generate-templated` operational. See [tasks.md](192-terrain-template-brush-generator/tasks.md) |
| 1 | [191 Procedural garden museum generator](191-procedural-garden-museum-generator/spec.md) | **In Progress: Blocked on Spec 192 terrain generator** | Requires full terrain generator and multi-tileset ADT pass from Spec 192 to replace legacy sand grid with lush walkable garden maps. See [tasks.md](191-procedural-garden-museum-generator/tasks.md) |
| 1 | [160 Skybox rendering](160-skybox-rendering/spec.md) | **Tasked: 72 tasks / 8 phases; 5 defects confirmed by source inspection** | Sky is a hardcoded 2-colour gradient today: authored colours are assigned then overwritten every frame (`WorldScene.cs:10566-10572`), the skybox model is night-gated (`:11882`), 2 of 5 authored bands are used, `MOSB` is reduced to a bool, and classification is filename matching. Next: **T001-T002** — close two automation gaps (`--time-of-day` pin; `profile-render` resolves its camera ONCE at `ProductionWorldSceneProfiler.cs:92` so its p99/max are not valid evidence), then capture the `Sky`/`SkyboxBackdrop` baseline. Unrecoverable once sky code changes; Phase 2 blocked on it. Rendering proof is automated via capture+pixel-diff, not handed to the user. See [tasks.md](160-skybox-rendering/tasks.md) |
| 1 | [184 PM4/PD4 generation from source geometry](184-pm4-generation-from-geometry/spec.md) | **Draft; built on a new corpus-wide decode** | Runs PM4 in the GENERATION direction instead of decode, which turns undecoded structure into a measurable residual. New 2026-08-23 evidence (`pm4 msur-window`, 309 non-empty files): `MSUR._0x18 + AttributeMask` is a running window that **exactly partitions MSLK** (517,783/517,783, sum = MSLK count, zero overrun, 100% coverage), and `MSLK.RefIndex` names a **neighbour** surface at **98.76% reciprocity** — so MSLK is the surface-adjacency graph and the documented "`_0x18` → MSCN" reading is eliminated (6,240 overruns, 93.52% coverage). 47.03% of edges carry the MSPV/MSPI wall quad, independently equal to the connective-geometry analyzer's 598,882. Next: **US1** — the conformance check, and FR-003's proof that it separates real files from `Pm4Generator`'s output (today: 1 MSLK record, 1 MSCN point, empty MSPV/MSPI, 100% triangles). **Ground truth is no longer scarce**: `pm4 object-library` maps 904 PM4 objects to 243 source assets (122 placed >1x), with tile + minimap coordinates per object. **Ceiling measured**: surface-count variance is **95.7% between-asset, 4.3% within-asset** (21 assets with >=8 placements), so a generator that is a pure function of the source model can reach ~95.7% and the contextual residual is ~4.3%. An earlier, stronger claim that a per-object generator "cannot reproduce these files" is **withdrawn**. What drives the 4.3% is open - rotation/placement-Z/bbox correlate weakly and inconsistently; terrain burial depth is untested and is the next measurement |
| 1 | [188 PM4 field semantics + grouping that tests itself](188-pm4-field-semantics/spec.md) | **Draft** | The viewer's PM4 grouping dropdown has 10 modes and **at least 4 group by a falsified reading**: `Ck24Type` is a float's **exponent band**, `Ck24ObjectId` its **mantissa bytes**, `AttributeMask` the **MSLK window length** (grouping by neighbour count). `MSLK.GroupObjectId`, used as an object identity throughout, is **near-unique per link** (1.622 links per distinct value) and groups nothing — its "99.9% distinctness" was distinctness without cardinality. Fix: make the dropdown an **instrument** that reports cardinality, group sizes, and purity+distinctness together (FR-004 — both halves of that trap were sprung this session), measured against a geometry-derived reference that uses no guessed field. Plus a sweep characterising every record field by **behaviour, not name**, gated on re-deriving the three already-settled fields (SC-007). The doodad population — **186,060 surfaces across 283 of 309 files** — currently has **no working grouping at all** |
| 1 | [190 Rosetta calibration corpus](190-rosetta-calibration-corpus/spec.md) | **Implemented with user gates: All 4 phases complete (US1–US4), 69/69 unit tests green** | Multi-version Zarr datastore, cross-era extension shifting (`.mdx` $\leftrightarrow$ `.mdl` $\leftrightarrow$ `.m2`), cross-build diff engine, first-class Alpha DBC/TRS generation, deterministic non-LLM PM4 lookup engine (`RosettaPm4LookupEngine`), and companion ADT synthesizer with SHA256 provenance tracking (`RosettaCompanionAdtSynthesizer`). All 69 Rosetta tests green. See [tasks.md](190-rosetta-calibration-corpus/tasks.md) |
| 1 | [186 Server data transformer + world content browsing](186-server-data-transformer/spec.md) | **Draft** | The tooling that turns any fan server's SQL into structured content the viewer can show — **NPCs, spells, quests, items and the dense text the viewer knows nothing about today**. Joins the two halves: client DBC (already wired — DBCD + 1,320 WoWDBDefs) holds spells/items/display; server SQL holds spawns, quest chains, loot, behaviour. **Toolmaker, not archivist** (FR-017): the operator supplies sources and owns their licensing. Lossless (FR-002, reconstruct-and-compare gated), attributed (FR-004), and **never normalised** (FR-005) — two projects disagreeing about one creature is the interesting content, not a conflict. `AlphaCoreDbReader` already proves no database engine is needed. Delivers standalone value with **no simulation required**. Consumes the 179–183 datastore; must not build a fourth store |
| 2 | [187 Single-player museum world simulation](187-museum-world-simulation/spec.md) | **Draft; depends on 186** | Makes something *happen* in the world 186 lets you see — deterministic single-player sim, no game protocol, no realm, no database at runtime, no client interception. Two spines: **determinism** (FR-001/007/012) makes the automated renderer-proof loop and model actors coexist by recording every model decision as an **input**, so a run replays without the model; the **integrity boundary** (FR-008/009/010) keeps models out of stored facts and invariants, allowing only validated+recorded open decisions and retrieval ranking. A small local model replaces the per-entity scripting layer. MCP is an adapter over the library surface, per 178's rule, with a deletion test |
| 1 | [151 WMO group admission](151-portal-game-mode-surface/spec.md) | **NEW owner of the remaining hitching, measured** | Stormwind submits all districts at once: 7512 visible groups, 80484 draw calls, and 100% of 592 hitches are `WmoSubmission` (p99 154ms, median 0.71ms). An ADMISSION problem, not batching. Instrument group admission (considered/admitted/rejected + which rule) before changing any logic |
| 2 | [153 Renderer hitch and MDX batching](153-renderer-hitch-and-batching/spec.md) | Phases 1/2/3/5 SHIPPED as v0.5.2.1 and confirmed by capture | Phase 4 likely moot (`SceneMaintenance` max 454.5 -> 3.9ms) — re-measure before implementing. Phase 5 step 2 (decode off the render thread) still owed: `DeferredAssetLoads` max 442.9ms vs a 3.5ms budget |
| 2 | [152 Renderer frame-time stability and per-era terrain lighting](152-renderer-frame-stability/spec.md) | Detector LANDED; Phase 1 refuted the allocation hypothesis so Phases 3-5 stay suspended in favour of 153. **Phase 6 terrain darkness: ROOT-CAUSED AND FIXED 2026-09-01; operator visual proof owed** | **MCNR component order is ERA-SPLIT, and the shared decoder used the alpha order for every era.** Measured with the new `inspect adt terrain-shading` on three clients, against convention-free ground truth (normals central-differenced from each tile's own heightmap, ~322k/193k samples): `0.5.3.3368` wants **(x,z,y)** (0.9976 vs 0.1809); `4.0.0.12025` and `5.0.1.15464` want **(x,y,z)** (0.9766/0.8366 vs 0.0303/0.2068). The inherited comment *"MCNR stores X, Z, Y (WoW convention)"* is true for alpha only. **Renderer agreement before -> after: Cata 0.0437 -> 0.9953, MoP 0.3282 -> 0.9985, alpha 0.9179 -> 0.9179 (deliberately untouched).** Fixed in the two STANDARD-path decoders only (`StandardTerrainAdapter.ExtractNormals`, `AdtTensorPackBuilder.AssembleNormals`); `AlphaWdtReader`/`AlphaTerrainAdapter` keep their own decode so 0.5.3 cannot regress. `TransformAdtNormalToRenderer` was NOT touched -- 0.5.3 renders correctly through it, so it was never implicated. **The test that should have caught this encoded the bug**: `AdtTerrainVertexLatticeTests` wrote a synthetic ADT to match the assumed convention then asserted it back (same shape as the BSDIFF tests); it now carries the measured numbers and an instruction to re-measure rather than edit expectations. Core.Tests **1282 passed / 9 pre-existing failures** (identical set). **Second, independent darkening term, NOT yet fixed**: MoP MCCV mask-gated mean **0.2675** vs neutral 0.4980, so the shader tint multiplies terrain by **0.53** -- a further **46.5%** on eras that have MCCV (0.5.3 has none). Decide whether that is authentic baked shading or a decode/normalisation fault before touching it. **Also still open**: `TerrainLighting` is a hardcoded time-of-day model with **no Light.dbc input at all**, identical for every era -- real work, but it was never the era split. **Next: operator loads a MoP/Cata map and confirms the terrain lights correctly.** See [[project_mcnr_axis_order_wrong]] |
| 3 | [151 Portal-aware rendering, game mode, and simple viewer surface](151-portal-game-mode-surface/spec.md) | Implementing; Phase 1 portal checkpoint passes source proof | Implement Phase 2 pure game-mode state/physics and character-head anchor; preserve editor camera state and stop at the focused physics checkpoint |
| 4 | [149 PM4 region navigation and audio trigger controls](149-pm4-region-audio-controls/spec.md) | Implementing; resident Zone/SubZone overlay slice wired, focused tests and PM4/audio stories open | Add area aggregation tests, then implement Phase 1 contracts/caller audit and validate region navigation before retiring correlation UI |
| 5 | [150 Alpha 0.5.3 renderer performance](150-alpha-renderer-performance/spec.md) | Draft evidence/planning pack complete; Spec 151 now owns its first portal-specific implementation slice | Keep broad profiling planning here; use Spec 151 for the bounded portal optimization and later return to repeated `profile-render` baselines |
| 6 | [148 Artifact world simulator runtime](148-world-simulator/spec.md) | Phase 1 audio diagnostics partially implemented; Alpha 0.5.3 MCSE and packed AreaNumber contracts landed | Add ZoneMusic row indirection, then finish source/read/decode coverage and user client inspection |
| 7 | [147 Minimap, fog, and doodad instancing](147-minimap-fog-instancing/spec.md) | Phase 2 implemented; Phase 3/4 open | User-run fullscreen drag/triple-click proof, then implement fog coverage before expanding doodad batch diagnostics |
| 8 | [146 Audio and camera playback](146-audio-camera-playback/spec.md) | Shared high/low AreaNumber Zone/SubZone selection and master mute control implemented; client audio contracts recovered | Add ZoneMusic row resolution; MIDI/DLS remains explicitly unsupported until a backend is proven |
| 9 | [144 Camera capture paths](144-camera-capture-paths/spec.md) | Implemented with user gates | Validate swept path residency during playback and confirm lease release before extensions |
| 10 | [145 WoW UI overhaul](145-wow-ui-overhaul/spec.md) | First slice implemented | Continue only the remaining persistent-window/placeholder audit tasks |
| 11 | [143 World context and lighting](143-world-context-lighting/spec.md) | LIT source/fallback and pre-alpha v2 parser implemented with user gate | Validate variant switching and no-Light-DBC fallback, then continue WMOAreaID and lighting evidence |
| 12 | [142 World scene graph](142-world-scene-graph/spec.md) | In progress | User-run dense-WMO capture to compare internal-doodad batch submissions against the previous placement-local path |
| 13 | [080 WoW UI consolidation](080-wow-ui-consolidation/spec.md) | Phase 2E Inspect/terrain page consolidation in progress | Validate Scene Placements/LOD, Experimental Terrain Lab tiles+clipboard, and Inspect Archeology dropdown |

## Other lanes

- Terrain/minimap reconstruction: Specs 139–141. Use only when the user names that lane or the
  status row in the selected task points there.
- Cross-era renderer research: Spec 138. Evidence/planning lane; do not generalize one client build
  to all 4.x clients.
- PM4: Specs 128–131 and `memory-bank/workstream-pm4-decode.md`; use the PM4 spec pack before code.
- PM4 region navigation/audio controls: Spec 149 owns the replacement UI and default-off trigger policy;
  it does not reopen PM4 decode or implement game mode/MIDI/DLS.
- Older numbered specs not listed above are background, superseded, or archived unless their task
  file is explicitly named.

## Status rules

- `Draft` means design exists; it is not implementation proof.
- `Implementing` means code may exist but required validation is open.
- `Implemented with user gates` means focused source/build proof exists and real-client proof remains.
- `Complete` requires the owning spec's validation gates to pass; do not infer it from task counts.
- When a spec becomes inactive, update this file and move detailed session history to its workstream
  or archive. Do not keep stale “current” prose in the agent guide.
