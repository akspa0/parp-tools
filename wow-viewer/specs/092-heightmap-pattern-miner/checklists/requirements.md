# Requirements Checklist: Heightmap Pattern Miner

- [x] Script path is under `wow-viewer/data-harvester/scripts/`.
- [x] Zarr `height_257` plus `index.parquet` is the input contract.
- [x] Map filters, patch sizes, stride, and tile limits are configurable.
- [x] Output includes ranked JSON and a visual atlas.
- [x] V23 training behavior is unchanged in this slice.

