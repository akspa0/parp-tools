# PM4 Restoration — pointer

PM4 restoration is an established research lane, not the default viewer handoff. Use the current
PM4 spec pack first:

- `specs/130-pm4-remaining-decode/`
- `specs/129-pm4-zarr-dataset/`
- `specs/128-pm4-negative-bsp-matching/`
- `memory-bank/workstream-pm4-decode.md`

The workstream contains durable findings, open questions, and validation commands. This pointer
contains no duplicated history. If the spec pack and workstream disagree, follow the owning spec
and record the correction in the workstream.
