# MPBP — Group Optional Portal/Batch Planes Block

## Summary
Second chunk in the `MPB*` optional sequence.

## Parent Chunk
`MOGP` (group file), optional.

## Build 0.7.0.3694 evidence
- Required immediately after `MPBV` in `FUN_006c1c60` when `MOGP.flags & 0x400`.

## Confidence
- Presence/order: **High**
