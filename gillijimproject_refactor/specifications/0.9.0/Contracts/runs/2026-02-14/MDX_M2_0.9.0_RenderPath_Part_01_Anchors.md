# MDX/M2 0.9.0 Render Path — Part 01 (Anchors)

## Scope
Small-scope checkpoint focused only on renderer-gate anchor discovery and symbol visibility.

## Commands/queries run
- `list_methods(offset=0, limit=400)`
- `list_exports(offset=0, limit=300)`
- `list_strings(filter=AddGeosetToScene)`
- `list_strings(filter=geoShared->)`
- `get_current_address()`
- `get_current_function()`

## Findings
- Methods page 0 is predominantly autogenerated names (`FUN_...`), no explicit model-render symbol names visible in this page.
- Export table returns only:
  - `entry -> 00401000`
- High-signal renderer assertions confirmed in string table:
  - `00822668`: `AddGeosetToScene: geoShared->materialId (%d) >= numMaterials (%d)`
  - `0082274c`: `geoShared->numVertices == 4`
- Current cursor context in Ghidra MCP:
  - address: `00401000`
  - function: `entry`

## Interpretation
- Render suppression evidence is still strong at string level.
- Named-function discovery via first methods page is insufficient; deeper localization requires xref-to-string resolution or direct address-to-function containment around anchor users.

## Next micro-step
- Attempt targeted function localization for string-anchor users and emit Part 02 with concrete candidate function addresses for gate branches.
