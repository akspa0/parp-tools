# MTEX — Texture Name Block

## Summary
String block containing terrain texture paths.

## Parent Chunk
Root-level ADT chunk.

## Build 0.7.0.3694 Evidence
- `FUN_006bd840` validates token `MTEX` from MHDR offset `piVar1[4]`.
- Texture loader is called with `mtexDataPtr` and `mtexChunkSize`.

## Confidence
- **High**
