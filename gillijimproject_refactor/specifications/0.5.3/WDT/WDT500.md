# WDT500 — 0.5.3 WDT Variant Marker

## Summary
Build-specific marker indicating an early monolithic map format path in 0.5.3.

## Parent Chunk
Root-level WDT loader context

## Builds Analyzed
| Build | Size | Notes |
|-------|------|-------|
| 0.5.3.3368 | variable | Marker string `WDT500` present; parser also validates WDT root + ADT-like chunks |

## Structure — Build 0.5.3.3368
| Offset | Type | Name | Description |
|--------|------|------|-------------|
| 0x00 | char[] | signature | Build-specific format selector (`WDT500`) used by loader-side logic |

## Observed Parse Domain (same client codebase)
- Root WDT token assertions: `MVER`, `MPHD`, `MAIN`, `MDNM`, `MONM`, plus alpha-era `MARE`/`MAOF`
- Embedded terrain/object token assertions: `MHDR`, `MCIN`, `MTEX`, `MCNK`, `MCLY`, `MCRF`, `MDDF`, `MODF`

This strongly suggests a monolithic map parse model in 0.5.3 where WDT processing transitions into ADT-like chunk handling.

## Version Differences
- **0.5.3 → 0.6.0**: likely transition toward split ADT/WDT semantics; exact branch logic pending.

## Ghidra Notes
- **Function address**: `???`
- **Parser pattern**: marker-driven WDT open/load via `wdtFile` + `%s\\%s.wdt`, then token-validated chunk parsing
- **Key observations**: not just a filename marker—likely tied to an older map container interpretation.

## Confidence
- **Medium** (marker + parse-domain behavior confirmed, low-level struct decode still in progress)

## References
- internal Ghidra strings: `0x0087C748`, `0x0089FB54`, `0x0089FB5C`, `0x0089FC54`, `0x008A2388`
